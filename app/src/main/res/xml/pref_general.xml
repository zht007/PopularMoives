<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="@string/pref_ordered_by_popularity"
        android:entries="@array/pref_ordered_by_list_titles"
        android:entryValues="@array/pref_ordred_by_list_values"
        android:key="@string/key_orded_by_list"
        android:title="@string/pref_title_ordered_by" />

</PreferenceScreen>
